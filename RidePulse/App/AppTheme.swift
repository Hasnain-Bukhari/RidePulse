import SwiftUI

enum AppTheme {
    static let accent = Color("AccentColor")
    static let driver = Color.blue.opacity(0.1)
    static let rider = Color.green.opacity(0.15)
    static let system = Color.gray.opacity(0.15)

    static let cornerRadius: CGFloat = 12
    static let spacing: CGFloat = 12
}

